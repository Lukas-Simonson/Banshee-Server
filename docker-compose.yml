services:
  app:
    image: banshee-server:latest
    build:
      context: .
    environment:
      LOG_LEVEL: info
      JWT_SECRET: Fnd76quFpRV980czzWeqUren8urpddLx6pD2YVTtO3U
      STORAGE_PATH: /storage
      METADATA_PATH: /metadata
    volumes:
      - /Users/lsimonson/Desktop/BansheeTest/storage:/storage
      - /Users/lsimonson/Desktop/BansheeTest/metadata:/metadata
    ports:
      - '6060:6060'
    command: ["serve", "--env", "production", "--hostname", "0.0.0.0", "--port", "6060"]

volumes:
    storage:
        type: none
