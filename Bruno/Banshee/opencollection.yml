opencollection: 1.0.0

info:
  name: Banshee
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  variables:
    - name: baseURL
      value: http://localhost:8080
bundled: false
extensions:
  ignore:
    - node_modules
    - .git
  presets:
    request:
      type: http
      url: "{{baseURL}}/api/"
