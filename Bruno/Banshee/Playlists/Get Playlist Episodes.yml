info:
  name: Get Playlist Episodes
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/api/playlists/:playlistID/episodes"
  params:
    - name: playlistID
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
