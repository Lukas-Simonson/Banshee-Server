info:
  name: Get Playlists
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/api/playlists"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
