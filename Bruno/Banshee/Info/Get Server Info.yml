info:
  name: Get Server Info
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/api/info"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  # Get Server Info

  Gets information about the server.
