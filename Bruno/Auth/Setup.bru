meta {
  name: Setup
  type: http
  seq: 3
}

post {
  url: {{baseURL}}/api/auth/setup
  body: json
  auth: inherit
}

body:json {
  {
    "username": "{{username}}",
    "password": "{{password}}",
    "role": "admin" // Always should be admin
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
