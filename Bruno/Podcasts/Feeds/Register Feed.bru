meta {
  name: Register Feed
  type: http
  seq: 2
}

post {
  url: {{baseURL}}/api/podcasts/feeds
  body: json
  auth: inherit
}

body:json {
  {
    "url": "{{rssFeedURL}}"
  }
}

vars:pre-request {
  rssFeedURL: 
}

settings {
  encodeUrl: true
  timeout: 0
}
