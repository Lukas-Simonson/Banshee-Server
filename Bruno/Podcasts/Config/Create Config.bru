meta {
  name: Create Config
  type: http
  seq: 1
}

post {
  url: {{baseURL}}/api/podcasts/:podcastID/config
  body: json
  auth: inherit
}

params:path {
  podcastID: 
}

body:json {
  {
    "title": "{{configTitle}}",
    "imageURL": "{{configImageURL}}",
    "description": "{{configDescription}}"
  }
}

vars:pre-request {
  configTitle: 
  configImageURL: 
  configDescription: 
}

settings {
  encodeUrl: true
  timeout: 0
}
